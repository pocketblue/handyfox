name: launcher
sources:
  - type: script
    dest-filename: launcher.sh
    commands:
      - #!/bin/bash
      - export TMPDIR=$XDG_CACHE_HOME/tmp
      - if [ -e /app/extra/firefox/firefox ] ; export firefox=/app/extra/firefox/firefox
      - if [ -e /app/lib/firefox/firefox ] ; export firefox=/app/lib/firefox/firefox
      - exec $firefox --name $FLATPAK_ID "$@"
buildsystem: simple
build-commands:
  - install -d /app/lib/ffmpeg
  - install -D -m755 launcher.sh /app/bin/launcher.sh
